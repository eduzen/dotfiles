# shellcheck shell=bash
# Managed by chezmoi as ~/.env with private permissions.
# Host-specific values intentionally default to the current environment or empty strings.

export AUTOSUGGESTION_HIGHLIGHT_COLOR="${AUTOSUGGESTION_HIGHLIGHT_COLOR:-8}"
export PYTHONBREAKPOINT="${PYTHONBREAKPOINT:-ipdb.set_trace}"
export PYTHONUNBUFFERED="${PYTHONUNBUFFERED:-1}"
export PYTHONDONTWRITEBYTECODE="${PYTHONDONTWRITEBYTECODE:-1}"
export DOCKER_BUILDKIT="${DOCKER_BUILDKIT:-1}"
export COMPOSE_DOCKER_CLI_BUILD="${COMPOSE_DOCKER_CLI_BUILD:-1}"

export EMAIL="${EMAIL:-}"
export STORAGE="${STORAGE:-}"
export TMP="${TMP:-}"
export DOWNLOADS="${DOWNLOADS:-}"
export COMPLETED="${COMPLETED:-}"
export MUSIC="${MUSIC:-}"
export MOVIES="${MOVIES:-}"
export TV="${TV:-}"
export BOOKS="${BOOKS:-}"
export TZ="${TZ:-}"
export UMASK="${UMASK:-}"
export SAMBAPASS="${SAMBAPASS:-}"
export PUID="${PUID:-}"
export PGID="${PGID:-}"
